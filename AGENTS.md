# 个人借还记账 PWA - 需求拆解文档

## 产品概述

- **产品类型**: 个人理财记账移动应用（PWA）
- **场景类型**: <scene_type>prototype-app</scene_type>
- **目标用户**: 需要管理个人借贷往来记录的用户，主要在 iPhone 移动端使用
- **核心价值**: 帮助用户清晰记录借出/借入/归还账目，追踪待收回欠款，本地数据安全存储
- **界面语言**: 中文
- **主题偏好**: user_specified（支持深色/浅色切换，用户可手动切换，偏好保存在 localStorage）
- **导航模式**: 路径导航
- **导航布局**: 底部 Tab 导航栏（移动端 App 风格，固定底部，高度 70px）

---

## 页面结构总览

> **说明**：此表为页面生成的唯一数据源，包含所有页面（一级+二级）。底部 Tab 导航包含 3 个一级页面，其余为二级页面。

| 页面名称 | 文件名 | 路由 | 页面类型 | 入口来源 |
|---------|-------|------|---------|---------|
| 统计页 | `StatsPage.tsx` | `/` | 一级 | 底部 Tab「📊统计」 |
| 记录页（人员列表） | `RecordsPage.tsx` | `/records` | 一级 | 底部 Tab「📋记录」 |
| 设置页 | `SettingsPage.tsx` | `/settings` | 一级 | 底部 Tab「⚙️设置」 |
| 人员详情页 | `PersonDetailPage.tsx` | `/person/:name` | 二级 | 记录页 → 点击人员卡片 |
| 编辑账目页 | `EditRecordPage.tsx` | `/edit/:id` | 二级 | 人员详情页 → 点击记录的「编辑」按钮 |
| 添加账目页 | `AddRecordPage.tsx` | `/add` | 二级 | 设置页 → 点击「添加新账目记录」按钮 |

---

## 页面布局建议

- **布局模式**: 移动端单栏流式布局 —— 整体为 iOS App 风格，页面内容垂直排列，底部固定 70px Tab 栏，页面底部预留安全边距避免遮挡
- **视觉重心**: 数据展示与账目操作并重——统计页以图表为视觉核心，记录页以人员列表卡片为核心，详情/编辑/添加页以表单为核心
- **结果承载区**: 统计页图表区（环形图+折线图）实时反映 localStorage 数据；记录页人员列表区动态渲染排序后的人员卡片；人员详情页记录列表区展示往来记录
- **初始态**: 无数据时统计页显示 0 金额 + 空状态图表占位，记录页显示"暂无往来人员"空状态提示

---

## 导航配置

> **说明**：底部 Tab 导航为全局导航，仅包含 3 个一级页面。二级页面通过页面内操作进入，顶部使用返回按钮导航。

- **导航布局**: 底部 Tab 栏（Bottom Tab Bar，固定底部，高度 70px）
- **导航项**（仅一级页面）:

| 导航文字 | 路由 | 图标 | 选中色 |
|---------|------|------|--------|
| 📊统计 | `/` | 图表图标 | #1677ff |
| 📋记录 | `/records` | 列表图标 | #1677ff |
| ⚙️设置 | `/settings` | 齿轮图标 | #1677ff |

- **二级页面导航**: 每个二级页面顶部左侧有返回按钮（<），点击返回上一级页面

---

## 数据来源声明

| 数据/操作 | 来源类型 | 实现要求 | mock 兜底 |
|---|---|---|---|
| 账目记录数据 | local-persist | localStorage key=`borrow_ledger_v2`，JSON 格式存储全部记录数组，启动时读取并异常兜底 | 初始为空数组，无预置 mock 数据 |
| 主题偏好 | local-persist | localStorage key=`ledger_dark_v2`，值为 `'dark'` / `'light'`，启动时读取决定初始主题 | 默认为浅色模式 |
| 导出 CSV 备份 | import-export | 将全部记录序列化为 CSV（含 ID、姓名、日期、金额、类型、渠道、还款日、备注、是否结清列），通过 Blob + a.click 触发下载 | 无 |
| 导入 CSV 恢复 | real-file + local-persist | 通过 `<input type="file">` 选择 CSV 文件，FileReader 解析后覆盖写入 localStorage | 导入失败时 toast 提示 |
| 人民币财务大写转换 | 纯前端计算 | 金额输入时实时调用转换函数，基于数字转中文大写规则生成 | 无（纯函数） |

---

## 功能列表

### 页面: 统计页 (`/`)

- **页面目标**: 以图表和数字卡片形式展示整体借还数据概览
- **功能点**:
  - **总借出金额卡片**: 汇总所有 `type=lend` 的记录金额，顶部大号数字展示
  - **待收回欠款卡片**: 汇总所有 `type=lend` 且 `isDone=false` 的记录金额，红色突出显示
  - **环形饼图 (Doughnut)**: 展示已收回金额（绿色）与待收回欠款（红色）的占比分布，带图例和百分比标注
  - **近6个月双折线图**: 平滑曲线（tension 效果）展示每月借出（红色）与归还（绿色）金额趋势，X 轴为最近 6 个月份，Y 轴为金额
  - **深色模式适配**: 图表在深色模式下自动调整坐标轴文字颜色和网格线颜色，保证可读性

### 页面: 记录页 (`/records`)

- **页面目标**: 按人员维度展示所有往来对象及欠款情况
- **功能点**:
  - **顶部统计栏**: 显示往来人员总数、总借出金额、待收回金额三项汇总数据
  - **人员卡片列表**: 按"待收回欠款金额"从大到小降序排列，每张卡片显示姓名、待收回金额（红色高亮）、总借出金额
  - **跳转人员详情**: 点击人员卡片跳转到 `/person/:name` 二级详情页
  - **空状态处理**: 无数据时显示友好的空状态提示文案

### 页面: 人员详情页 (`/person/:name`)

- **页面目标**: 展示单个人员的所有往来记录详情及操作
- **功能点**:
  - **人员信息头**: 显示姓名、总借出金额、待收回金额小计
  - **返回导航**: 顶部左侧返回按钮，点击回到记录页
  - **往来记录列表**: 按日期倒序排列，每条记录显示类型标签（借出红/借入紫/归还绿）、金额、交易日期、支付渠道、备注
  - **状态标签**: 借出未结清且已过还款日 → 红色"已逾期"；已结清 → 绿色"已结清"
  - **标记结清**:
    - 触发: 记录行内「标记结清」按钮（仅借出未结清记录显示）
    - 交互: 点击后确认或直接切换状态
    - 提交: 更新该记录 `isDone=true`，同步写入 localStorage
    - 反馈: toast 提示"已标记为结清" + 列表即时更新
    - 数据契约: `IRecord.isDone: boolean`
  - **编辑记录**: 点击「编辑」按钮跳转到 `/edit/:id` 编辑账目页
  - **删除记录**:
    - 触发: 记录行内「删除」按钮
    - 交互: 弹出确认对话框
    - 提交: 从数组中移除该记录，同步 localStorage
    - 反馈: toast 提示"删除成功" + 列表更新
    - 数据契约: 按 `id` 定位删除

### 页面: 编辑账目页 (`/edit/:id`)

- **页面目标**: 修改已有账目的全部字段
- **功能点**:
  - **表单字段**: 姓名、交易日期、金额、账目类型（借出/借入/归还）、支付渠道（微信/支付宝/银行卡）、约定还款日、备注
  - **金额财务大写**: 金额输入框下方实时显示人民币财务大写（如"人民币壹仟贰佰叁拾肆元伍角陆分"）
  - **保存提交**:
    - 触发: 页面底部「保存」按钮
    - 交互: 校验必填字段
    - 提交: 更新记录对象，写入 localStorage
    - 反馈: toast 提示"保存成功" + 返回到对应人员详情页
  - **返回导航**: 顶部返回按钮，点击返回人员详情页

### 页面: 添加账目页 (`/add`)

- **页面目标**: 新增一条账目记录
- **功能点**:
  - **表单字段**: 对方姓名（必填）、交易日期（必填，默认今天）、金额（必填，>0）、账目类型（借出/借入/归还，默认借出）、支付渠道（微信/支付宝/银行卡，必填）、约定还款日（仅借出类型显示，选填）、备注（选填）
  - **类型联动**: 账目类型切换为"借出"时显示还款日输入框，其他类型隐藏
  - **金额财务大写**: 金额输入时下方实时显示人民币财务大写
  - **必填校验**:
    - 触发: 点击「提交」按钮
    - 校验: 姓名、日期、金额、支付渠道不能为空；金额必须 > 0
    - 反馈: 未通过时对应输入框红色边框高亮 + alert 弹窗汇总所有缺失项
  - **提交保存**:
    - 触发: 校验通过后
    - 提交: 生成新记录（id 为时间戳），追加到数据数组，写入 localStorage
    - 反馈: toast 提示"添加成功" + 清空表单 + 跳回设置页
  - **返回导航**: 顶部返回按钮，点击返回设置页

### 页面: 设置页 (`/settings`)

- **页面目标**: 提供数据管理和主题切换入口
- **功能点**:
  - **添加新账目**: 点击按钮跳转到 `/add` 添加账目页
  - **导出 CSV**:
    - 触发: 点击「导出CSV备份文件」按钮
    - 交互: 生成 CSV 字符串（列：ID、姓名、日期、金额、类型、渠道、还款日、备注、是否结清）
    - 提交: 通过 Blob + URL.createObjectURL + a.click 触发下载
    - 反馈: toast 提示"导出成功"
  - **导入 CSV**:
    - 触发: 点击「导入CSV恢复数据」按钮 → 选择文件
    - 交互: 隐藏的 `<input type="file" accept=".csv">` + FileReader 解析
    - 提交: 解析后的数据覆盖 localStorage 中 `borrow_ledger_v2`
    - 反馈: 成功 toast + 刷新页面数据；失败 alert 提示"文件格式错误"
  - **主题切换**:
    - 触发: 点击「深色/浅色模式」切换按钮或开关
    - 提交: 切换全局主题 class，写入 localStorage `ledger_dark_v2`
    - 反馈: 页面即时切换深色/浅色样式，图表同步适配

---

## 数据共享配置

| 存储键名 | 数据说明 | 使用页面 |
|---------|---------|---------|
| `borrow_ledger_v2` | 全部账目记录数组，类型为 `IRecord[]` | 统计页、记录页、人员详情页、编辑页、添加页、设置页 |
| `ledger_dark_v2` | 主题模式，类型为 `'dark' \| 'light'` | 所有页面 |

```ts
interface IRecord {
  /** 记录唯一ID，时间戳 */
  id: string;
  /** 对方姓名 */
  name: string;
  /** 交易日期 YYYY-MM-DD */
  tradeDate: string;
  /** 金额（元） */
  money: number;
  /** 账目类型：lend=借出 / borrow=借入 / return=归还 */
  type: 'lend' | 'borrow' | 'return';
  /** 支付渠道 */
  channel: '微信' | '支付宝' | '银行卡';
  /** 约定还款日（仅借出类型有值） */
  repayDate?: string;
  /** 备注说明 */
  note?: string;
  /** 是否已结清 */
  isDone: boolean;
}
```

---

## 设计规范要点

- **整体风格**: iOS App 风格，大圆角卡片（16px）、柔和阴影、合理间距、系统字体 PingFang SC
- **主色调**: 蓝色 #1677ff
- **语义色**: 借出/危险红色 #f53f3f、归还/成功绿色 #00b42a、借入紫色 #722ed1
- **底部 Tab 栏**: 固定底部，高度 70px，选中项蓝色高亮
- **输入框**: 高度 48px，圆角 12px，聚焦时边框变蓝
- **按钮**: 高度 48px，圆角 12px，点击时有透明度反馈
- **适配**: viewport 禁止缩放，适配 iPhone 移动端，页面底部预留安全边距避免 Tab 栏遮挡
- **交互**: 页面切换流畅无闪烁

-------

<scene_type>prototype-app</scene_type>

# UI 设计指南

## 1. 设计推导依据

- **参考意图**: Free Direction —— 用户提供完整功能需求与设计参数，无视觉参考图，按产品语义自主设计
- **核心情绪 / 应用类型**: 个人财务记账工具，追求清晰可信赖、操作轻盈、账目一目了然
- **独特记忆点**: 三色账目状态标签（借出红 / 借入紫 / 归还绿）贯穿列表与图表，配合 iOS 风格大圆角卡片与毛玻璃底 Tab 栏

## 2. Art Direction

- **方向名**: iOS Soft Cards
- **Design Style**: Rounded 圆润几何 + Soft Blocks 柔色块 —— 贴合 iPhone 原生 App 视觉预期，大圆角卡片、柔和阴影、轻量色彩分区
- **DNA 参数**: 圆角 16px（卡片）/ 12px（按钮输入框）；阴影 subtle ~ layered；间距 standard（16px 基础步长）；字体方向 中文无衬线 + 数字高可读性；装饰手法 纯色块标签、轻量毛玻璃 Tab 栏
- **应用类型**: Tool —— 移动端单列流式布局，底部 Tab 主导航，二级页面顶部返回

## 3. Color System

**色彩关系**: 品牌蓝主色 + 三色账目状态语义色（借出红/归还绿/借入紫） + 浅灰卡片背景 / 深色模式深空灰底
**配色设计理由**: 蓝色承担主交互与品牌锚点（CTA、Tab 激活、聚焦边框）；账目状态用独立三色做快速识别；中性色保持克制以突出金额与状态信息
**主色推导**: 以用户指定 #1677ff 为 primary 锚点，语义色沿用 #f53f3f / #00b42a / #722ed1，确保与需求完全一致
**使用比例**: 65% 中性 / 25% 辅助（语义色）/ 10% primary；primary 仅用于主按钮、Tab 激活、聚焦态，不与语义色混用

| 角色 | CSS 变量 | Tailwind Class | HSL 值 | 设计说明 |
|---|---|---|---|---|
| bg | `--background` | `bg-background` | hsl(210 20% 98%) | 页面背景，浅灰偏冷 |
| card | `--card` | `bg-card` | hsl(0 0% 100%) | 卡片、表单、列表项承载面 |
| text | `--foreground` | `text-foreground` | hsl(222 20% 12%) | 标题、金额、正文 |
| textMuted | `--muted-foreground` | `text-muted-foreground` | hsl(215 10% 50%) | 说明文字、日期、备注 |
| primary | `--primary` | `bg-primary` / `text-primary` | hsl(216 100% 52%) | 主行动、Tab 激活、输入聚焦 |
| primaryForeground | `--primary-foreground` | `text-primary-foreground` | hsl(0 0% 100%) | primary 上的文字图标 |
| accent | `--accent` | `bg-accent` | hsl(216 30% 95%) | hover / 选中浅底、菜单项状态 |
| accentForeground | `--accent-foreground` | `text-accent-foreground` | hsl(216 30% 28%) | accent 上的文字图标 |
| border | `--border` | `border-border` | hsl(216 10% 90%) | 卡片边界、分隔线、输入框默认态 |

**语义色提示**:
- 借出/危险（red）：bg hsl(0 90% 96%) / border hsl(0 85% 70%) / text hsl(0 90% 55%) —— 用于借出金额高亮、逾期标签、删除操作；饱和度与 primary 对齐
- 归还/成功（green）：bg hsl(135 75% 95%) / border hsl(135 60% 65%) / text hsl(145 80% 38%) —— 用于归还记录、已结清标签、回收曲线
- 借入（purple）：bg hsl(270 60% 95%) / border hsl(270 50% 70%) / text hsl(270 70% 50%) —— 用于借入类型标签与对应记录
- 深色模式下 bg / card / border 整体降明度，语义色饱和度微调保持 +10% 以确保在深色底上的可读性

## 4. 字体与节奏

- **font-display**: Noto Sans SC —— 中文标题清晰现代，符合 iOS 系统字体气质（PingFang SC 作为 fallback）
- **font-body**: Noto Sans SC —— 正文、列表、表单统一无衬线，数字易读
- **字号**: 页面大标题 text-2xl；卡片标题 text-lg；金额数字 text-xl ~ text-3xl（粗体）；body text-base；muted text-sm
- **圆角**: 大 —— 卡片 16px、按钮输入框 12px、标签 8px，贴合 iOS 视觉语言

## 5. 全局布局契约

- **Reference Layout Use**: 按需求结构推导
- **Page / Section Order**: 统计页 → 记录页 → 设置页（底部 Tab）；二级页：人员详情、添加账目、编辑账目（顶部返回）
- **Standard Content Zone**: max-w-md mx-auto（移动端单列），适配 iPhone 宽度，大屏维持舒适阅读宽度
- **Shell / Frame Alignment**: 安全区独立网格 —— 底部固定 Tab 栏（高 70px + safe-area-inset-bottom），内容区独立滚动，底部预留 90px 内边距避免遮挡
- **Padding & Rhythm**: 页面水平 px-4，垂直 py-4；卡片间距 gap-3 ~ gap-4；保持 8px 倍数节奏
- **Full-bleed Zones**: 无全宽 Hero；图表容器随内容区宽度
- **Local Narrowing**: 表单页内边距 px-4，字段全宽展开
- **Overflow Strategy**: 列表纵向滚动；图表宽度自适应容器
- **Flexibility Boundary**: 允许小屏设备微调卡片内边距与字号；不允许改变圆角、主色、阴影语言与 Tab 高度

## 6. 视觉与动效

- **装饰**: 纯色状态标签 + 轻量毛玻璃 Tab 栏
- **阴影/边界**: 轻 —— 卡片 shadow-sm（0 2px 8px rgba(0,0,0,0.04)），深色模式下弱化阴影强化边界
- **动效**: 精致克制 —— 页面切换 200ms 淡入淡出；按钮点击 opacity 0.7 反馈；Tab 切换 150ms 平滑过渡；图表入场轻量渐显

## 7. 组件原则

- 按钮高 48px、圆角 12px；Primary 实心蓝；Danger 红底白字（删除/标记结清）；Secondary 灰底黑字（取消/次要操作）
- 输入框高 48px、圆角 12px、默认浅灰边框、聚焦时 primary 边框 + 轻微外发光
- 卡片统一 16px 圆角、白底轻阴影，列表卡片点击有 accent 背景反馈
- 状态标签：pill 形状、文字 12px、左右 padding 8px、上下 3px，三色对应三类账目状态
- 空状态与加载状态延续卡片风格，使用 textMuted 与轻量图标

## 8. Image Direction

- **Image Role**: 无强制图片需求，优先通过排版、色彩状态标签和图表建立视觉记忆点
- **Image Art Direction**: 无
- **Image Prompt Keywords**: 无
- **Image Avoidance**: 避免通用金钱/账本类插画素材，避免无意义渐变背景

## 9. Anti-patterns

- **Split personality**: 不同页面使用不同圆角、阴影或卡片密度；全站统一 16px 卡片圆角与轻阴影
- **Status color overload**: 状态色同时用于按钮、文字、背景、边框、图表全部位置；按角色分配（标签/金额/图表系列/按钮）
- **iOS drift**: 使用安卓 Material 风格的凸起按钮或尖角设计；始终保持大圆角、柔和阴影、系统字体质感
- **Invisible focus**: 只做 hover 不做 focus-visible；输入框与按钮必须有清晰的键盘聚焦态（primary 边框）
- **Tab overlap**: 内容列表底部被 Tab 栏遮挡；必须预留 safe-area + 70px 底部内边距
- **Dark mode afterthought**: 深色模式只改背景不改图表与语义色饱和度；需同步提升语义色明度差以保证可读性